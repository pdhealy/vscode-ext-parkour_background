Finish the task of executing the prompt in file `Private/dev_projects/vscode/extensions/editor_background_image/docs/planning/prd/prd_v2.md`. You must use the sequential-thinking and context7 MCP servers. You must create a ToDo list of your actions items and their completion.

Ok, the code is now written based on the requirements in file `Private/dev_projects/vscode/extensions/editor_background_image/docs/planning/prd/prd_v2.md`. Please verify that the code is correct and meets the specifications outlined in the PRD. VSCode is installed on this device where the code exists for testing.


Only implement support for .webp images as the background image format. Ensure that the extension correctly handles .webp files and applies them as background images in VSCode. Test the functionality to confirm that .webp images are displayed properly as background images when selected by the user. Replace the cat image with file `Private/dev_projects/vscode/extensions/editor_background_image/assets/animated-webp-supported.webp`


Okay, let's save this code to a new public GitHub repository at https://github.com/pdhealy. The repository will be named `vscode-ext-parkour_background`. Update Private/dev_projects/vscode/extensions/editor_background_image/.gitignore first with all required files and folders to be ignored. Then, create the new repository and push the code to it.


The `Background Image: Toggle` Command Pallete option is not working correctly see image Private/dev_projects/vscode/extensions/editor_background_image/docs/screenshots/error_8.png . The Toggle does not turn off the background image when it is on. Please fix this issue and ensure that the Toggle functionality works as expected. Additionally, please verify that the background image is properly applied when the Toggle is turned on.

ffmpeg -i '7 Minutes Minecraft Parkour Gameplay [Free to Use] [Map Download] [7BZ5ja3oS3Q].mkv' -vf "fps=10,scale=640:-1" output.webp

Update the following command to split the input video into multiple .webp files, each containing a portion of the video. The output files should be named sequentially (e.g., output_001.webp, output_002.webp, etc.). The command should be modified to include a segmenting option that allows for splitting the video into parts of a specified duration (e.g., 10 seconds each). The output should be based on your previous recommendation like Animated WebP (longer loop) │ 640×360                │ 100–500KB


Update the project code based on the following requirements:
- Update the Command Pallete option "Background Image: Toggle" to "Background Image (Minecraft): Toggle"
    - When the Command Pallete option "Background Image (Minecraft): Toggle" is turned / toggled on, it must load a random background (.webp) image from the `assets/minecraft` folder and apply it as the background image in VSCode. You must advise me if the filenames inside the `assets/minecraft` folder need to be updated to ensure that they are in the correct format and compatible with the extension's functionality and for the code to select a random image correctly. The filenames should be in a consistent format (e.g., image1.webp, image2.webp, etc.) to facilitate the random selection process.
    - When the Command Pallete option "Background Image (Minecraft): Toggle" is turned / toggled off, it must remove the background image and revert to the default VSCode background.
- Add a new Command Pallete option called "Background Image (Subway Surfers): Toggle".
    - When the Command Pallete option "Background Image (Subway Surfers): Toggle" is turned / toggled on, it must load a random background (.webp) image from the `assets/subway_surfers` folder and apply it as the background image in VSCode. You must advise me if the filenames inside the `assets/minecraft` folder need to be updated to ensure that they are in the correct format and compatible with the extension's functionality and for the code to select a random image correctly. The filenames should be in a consistent format (e.g., image1.webp, image2.webp, etc.) to facilitate the random selection process.
    - When the Command Pallete option "Background Image (Subway Surfers): Toggle" is turned / toggled off, it must remove the background image and revert to the default VSCode background.
- Remove the existing fallback to the image `assets/image.png` if the extension fails to load a background image using the new toggle functionality. Instead, if the extension fails to load a background image, it should simply not apply any background image and revert to the default VSCode background while displaying a standard error message in the bottom right corner of VSCode indicating that the background image failed to load. The error message should be clear and concise, informing the user of the issue without causing confusion or disruption to their workflow. Ensure that the error handling is implemented gracefully, allowing users to continue using VSCode without any interruptions while being informed of the background image loading issue.
- Don't break any existing functionality in the extension while implementing these changes. Ensure that the new functionality is added seamlessly without affecting the current features of the extension. Test the extension thoroughly to confirm that all features, including the new toggle functionality, work as expected without any issues.


Make the following changes to the new opacity settings:
- Minimum value should be 0.05 (5% opacity) instead of 0.15 (15% opacity).
- Maximum value should be 0.25 (25% opacity) instead of 1 (100% opacity).
- The default value should be 0.15 (15% opacity) instead
- The opacity settings should be applied uniformly to both the Minecraft and Subway Surfers background images when they are toggled on. Ensure that the opacity settings are correctly applied and that the background images are displayed with the specified opacity levels when selected by the user. Test the functionality to confirm that the opacity settings work as intended and that the background images are displayed with the correct opacity when toggled on.


Make the following changes to the VSCode extension:
- Change the name of the extension to "Parkour Background" to better reflect the theme of the background images being used. Update all relevant files, including the extension manifest (package.json) and any documentation, to reflect the new name. Ensure that the new name is consistent across all platforms where the extension is available, including the Visual Studio Code Marketplace. Test the extension after making these changes to confirm that it functions correctly with the new name and that there are no issues with loading or applying background images.
- Update the Command Pallete options to reflect the new name of the extension. For example, change "Background Image (Minecraft): Toggle" to "Parkour Background (Minecraft): Toggle On/Off" and "Background Image (Subway Surfers): Toggle" to "Parkour Background (Subway Surfers): Toggle On/Off", and "Background Image: Set Opacity" to "Parkour Background: Set Opacity". Ensure that the new Command Pallete options are clear and descriptive, indicating the functionality of each option while maintaining consistency with the new extension name. Test the Command Pallete options after making these changes to confirm that they work as expected and that users can easily understand and use the new options to toggle background images and set opacity levels.
- Ensure that the background is set to the default VSCode background when VSCode first loads, and that the background image is only applied when the user toggles it on using the Command Pallete options. This means that when the extension is activated, it should not apply any background image until the user explicitly chooses to do so by toggling the background image on. Test this functionality to confirm that the default VSCode background is displayed upon loading and that the background image is only applied when toggled on by the user.

Thanks, it's fixed now. The bankground image disappears anytime I click or select anywhere outside of an active editor window. Please fix this issue so that the background image remains visible in the previous active editor window regardless of where the user clicks or selects within VSCode, as long as the background image toggle is turned on. Test the functionality to confirm that the background image stays visible and does not disappear when interacting with different areas of VSCode.

Thanks that worked, but now the background image is in all editor windows instead of previously where it would only appear in the editor window where the cursor is active. Please update the functionality so that the background image only appears in the currently active editor window, and not in all editor windows. Ensure that when the user clicks or selects a different editor window, the background image should only be applied to the newly active editor window while being removed from the previously active editor window. Test this functionality to confirm that the background image behaves as expected, appearing only in the active editor window and updating correctly when switching between editor windows.

Thanks that worked, however now the Toggle On/Off functionality is not working correctly and is very slow wheras it was working fine previously. When I toggle the background image off, it does not remove the background image from the active editor window. Please fix this issue so that when the user toggles the background image off, it correctly removes the background image from the active editor window and reverts to the default VSCode background. Test the functionality to confirm that toggling off the background image works as expected and that the background image is removed properly when toggled off. When I try to toggle to background image on again, I have to select the background image toggle option twice for it to work. Please fix this issue so that the user can toggle the background image on and off with a single selection of the Command Pallete option, without needing to select it multiple times. Test the functionality to confirm that toggling the background image on and off works smoothly and as expected with a single selection of the Command Pallete option. Please review my previous code commits to see how this was handled previously when it was working correctly and ensure that the current implementation is updated to reflect the previous working functionality while maintaining the new features and improvements that have been added. Test the extension thoroughly to confirm that all functionalities, including toggling the background image on and off, work as expected without any issues or delays.


Develop unit tests for all core functionalities of the extension, including:
- Toggling the background image on and off for both Minecraft and Subway Surfers themes.
- Setting the opacity of the background images.
- Handling errors when loading background images.
- Ensuring that the background image is applied only to the active editor window and updates correctly when switching between editor windows.
- Verifying that the default VSCode background is displayed upon loading and that the background image is only applied when toggled on by the user.
Use a testing framework such as Mocha or Jest to write and run the unit tests. Ensure that the tests cover all edge cases and scenarios to validate the functionality and robustness of the extension. Run the tests after implementing them to confirm that all tests pass successfully and that the extension functions correctly under various conditions. 


Prepare my VSCode extensin for release and publishing on the Visual Studio Code Marketplace. This includes:
- Updating the extension manifest (package.json) with all necessary information, including the new name "Parkour Background", description, version number, author information, and any relevant keywords to improve discoverability on the marketplace.
- Creating a visually appealing and informative README.md file that provides an overview of the extension, its features, installation instructions, usage guidelines, and any other relevant information to help users understand the extension and how to use it effectively.
- Adding screenshots or demo videos to the README.md file to showcase the functionality of the extension and provide users with a visual representation of what to expect when using the extension.
- Ensuring that all documentation is clear, concise, and free of errors to provide a professional and polished presentation of the extension on the marketplace.
- Highlight any code that can be removed or refactored to improve the performance and maintainability of the extension before publishing. This includes removing any unused code, optimizing existing code, and ensuring that the codebase is clean and well-organized. DO NOT break any existing functionality while making these improvements. Test the extension thoroughly after making any changes to confirm that all features work as expected and that there are no issues or regressions introduced during the refactoring process.

Commit and push all changes to the remote repository, and tag the commit as 'working' or whatever the best practise is.